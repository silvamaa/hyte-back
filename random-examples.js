const doSomething = (input) => 'i did something' + input;
// console.log(doSomething('eat'));

const doubles = [1, 2, 3].map((num) => num * 2);
// console.log(doubles);

export {doSomething, doubles};
